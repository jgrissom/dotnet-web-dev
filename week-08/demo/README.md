# Week 8 Demo — Curbside Gets the Rest of CRUD ✏️🗑️

The live-coding companion to week 8: the scaffolder writes a complete CRUD controller for the food-truck app, the room reads it, and Edit and Delete get ported into Curbside's own controller — then the model grows a `Slogan` column on a table that already has rows.

- **[`demo-script.md`](demo-script.md)** — the cue sheet, keyed to the slides ([clickable version](https://jgrissom.github.io/dotnet-web-dev/week-08/demo/script.html) with checkboxes and Copy buttons)
- **The starting code** (`week-08/demo-starter/Curbside`) lives in the private answer-keys repo — same reason as weeks 5–7: a finished database-backed app with a validated form is next year's answer keys rolled together
- **Students: lids down.** The demo app isn't in this repo, deliberately — you'll run the scaffolder yourself in the lab, on the Registry, with `dotnet test` telling you how it's going
